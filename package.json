{
  "name": "roubaai-media",
  "version": "0.1.2-rc.6",
  "private": true,
  "description": "RoubaAI media generation plugin family for the DeepSeek Harness: a provider seam (ctx.media) plus swappable image/video/music backends and the Settings page that configures them.",
  "type": "module",
  "license": "MIT",
  "packageManager": "pnpm@11.8.0",
  "engines": {
    "node": "^22.19.0 || >=24"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "test": "vitest run",
    "clean": "pnpm -r exec node -e \"require('node:fs').rmSync('lib',{recursive:true,force:true})\"",
    "sync:canvas-frontend": "node packages/canvas/scripts/sync-frontend.mjs",
    "check:canvas-frontend": "node packages/canvas/scripts/sync-frontend.mjs --check",
    "sync:brand-logo": "node packages/brand/scripts/sync-logo.mjs",
    "check:brand-logo": "node packages/brand/scripts/sync-logo.mjs --check",
    "pack:all": "pnpm run check:brand-logo && pnpm run sync:canvas-frontend && pnpm -r exec pnpm pack --pack-destination ../../dist"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "@types/react": "~18.3.1",
    "lightningcss": "^1.32.0",
    "react": "^18.2.0",
    "tsdown": "^0.22.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
